Config(
    x: Fraction(0.5),
    y: Fraction(0.05),
    width: Fraction(0.35),
    height: Fraction(0),
    hide_icons: false, 
    ignore_exclusive_zones: false, 
    layer: Overlay, 
    hide_plugin_info: true, 
    close_on_click: true,
    show_results_immediately: true,
    max_entries: None,
    plugins: [
        "libapplications.so",
        "libsymbols.so",
        "librink.so",
        "libdictionary.so",
        "libtranslate.so",
    ],
)
